import * as React from 'react';

import { RootWrapper } from '@/components/elements';
import ExampleParts from '@/components/section/BerandaPage/parts/ExampleParts';

const Beranda = () => {
  return (
    <RootWrapper>
      <ExampleParts />
      <ExampleParts />
      <ExampleParts />
    </RootWrapper>
  );
};

export default Beranda;
